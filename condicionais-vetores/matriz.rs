fn main(){
    // Definindo a matriz array
    let semana = ["segunda", "terça", "quarta", "quinta", "sexta", "sabado", "domingo"];
    // Definindo array apenas com valor inicial e tamanho máximo
    let numeros = [0; 5];
    // Acessando elementos do array
    let inicio = semana[0];
    let fim = semana[6];
    println!("Inicio da semana {}. Fim da semana {}", inicio, fim);
    // Inicio da semana segunda. Fim da semana domingo
    println!("Numeros inclusos {}", numeros[0]);
    //Numeros da matriz
}
